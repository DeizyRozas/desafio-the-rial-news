<%= form_with(model: comentario) do |form| %>
  <% if comentario.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(comentario.errors.count, "error") %> prohibited this comentario from being saved:</h2>

      <ul>
        <% comentario.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field my-3">
    <%= form.label :Tu_comentario, style: "display: block", class:"mb-2" %>
    <%= form.text_field :contenido, class:"form-control"  %>
  </div>

  <div>
    <%= form.submit class:"btn btn-sm btn-primary my-3" %>
  </div>
<% end %>
