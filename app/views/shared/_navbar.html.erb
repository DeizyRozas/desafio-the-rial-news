<nav class="navbar navbar-expand-lg navbar-light nav" id="mainNav">
    <div class="container px-4 px-lg-5">
        <a class="navbar-brand" href="/">The Rial News</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarResponsive" aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">
            Menu
            <i class="fas fa-bars"></i>
        </button>
        <div class="collapse navbar-collapse" id="navbarResponsive">
            <ul class="navbar-nav ms-auto py-4 py-lg-0">

                <% if user_signed_in? %>
                    <li class="nav-item"><a class="nav-link px-lg-3 py-3 py-lg-4" href="noticia">Noticias</a></li>
                <%end%>

                <% if user_signed_in? %>
                <li class="nav-item">
                    <a class="nav-link  px-lg-3 py-3 py-lg-4" href="#" >
                    <%= current_user.email%>
                    </a>
                </li>
                <%end%>

                <% if user_signed_in? %>
                <li class="nav-item">
                    <%= button_to 'Cerrar sesión' , destroy_user_session_path, class: "btn btn-outline-light", method: :delete %>
                </li>
                <% else %>
                <li class="nav-item">
                    <%= link_to 'Iniciar sesión' , new_user_session_path, class: "nav-link px-lg-3 py-3 py-lg-4" %>
                </li>
                <li class="nav-item">
                    <%= link_to 'Registro' , new_user_registration_path, class: "nav-link px-lg-3 py-3 py-lg-4"  %>
                </li>
                <% end %>
            </ul>
        </div>
    </div>
</nav>

